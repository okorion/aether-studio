/** Shared world anchor for the reactor aperture, grains and later chamber light. */
export const REACTOR = {
  worldY: -40.4,
  heightScale: .76,
  apertureY: 3.15,
  apertureRadius: 1.12,
  capRadius: 1.93,
  capThickness: .26,
  ringScale: 2 / 3,
} as const
