# Aether Studio 작업 규칙

## 공개 범위와 개발 기준

- 사용자는 2026-09-22에 현재 GitHub 공개 상태가 본인이 직접 설정한 것임을 확인했다. 2026-09-23 중 직접 비공개로 전환할 예정이며, 이후에도 언제든 다시 공개할 수 있다. 최초 private 저장소 요청과 현재 공개 상태의 차이만으로 작업을 중단하거나 공개 승인을 반복해서 묻지 않는다.
- 위 일정은 계획이며 전환 완료의 증거가 아니다. 공개 범위가 필요한 작업에서는 실제 상태를 조회한다. 별도 변경 요청 없이 에이전트가 visibility를 바꾸거나 전환을 예약하지 않는다.
- 공개·비공개 상태와 관계없이 모든 새 커밋, PR 본문·댓글, CI 출력, 문서, 성능 자료와 캡처는 공개 가능한 내용으로 작성한다. 비공개 기간을 민감정보 보관 기간으로 사용하지 않는다.
- 토큰, 비밀번호, 개인 키, 인증 쿠키, 복구 코드, 실제 개인정보, 회사 내부 자료와 비공개 대화 원문은 Git 이력·배포 산출물·로그·이미지에 넣지 않는다. 예제와 테스트에는 가짜 값을 쓰고 필요한 비밀값은 로컬 환경 또는 배포 서비스의 비밀 설정에서 관리한다. 브라우저에 전달되는 환경변수와 번들은 공개 정보로 취급한다.
- 커밋 전 staged diff와 추가 파일 목록을 확인하고, 푸시 전 전송할 커밋 전체를 확인한다. 캡처는 화면 내용과 식별정보를 확인하고, 외부 에셋·폰트·영상·참고 사이트 자료는 출처와 재배포 조건을 확인한다. 참고 사이트를 볼 수 있다는 사실만으로 해당 코드·에셋의 재배포를 허용된 것으로 판단하지 않는다.
- `.gitignore`는 이미 추적 중인 파일이나 과거 이력을 보호하지 않는다. 민감정보가 발견되면 관련 푸시를 중단하고 위치와 범위를 값 노출 없이 보고한다. 파일 삭제만으로 과거 노출이 해결됐다고 보고하지 않는다.
- 공개 저장소라는 이유만으로 오픈소스 라이선스를 임의로 부여하거나 기존 라이선스를 바꾸지 않는다. 이 공개 승인은 별도 Career OS 규칙의 외부 경력 문서 사용 제한을 변경하지 않는다.

- GitHub 저장소 About의 description은 `Interactive 3D`로 고정한다. 사용자가 변경을 요청하기 전에는 키워드·구현 단계·부연 설명을 덧붙이지 않는다.

- README·저장소 소개·개발 문서는 [문서 작성 기준](docs/writing-guide.md)을 따른다. 소개는 사용자 동작과 실행 방법부터 쓰고, 구현 단계·PR·검사 횟수는 필요한 검증 기록에서만 다룬다. 현재 안내와 과거 측정 기록을 구분한다.

- 장면·오브젝트를 설명할 때 [장면 용어집](docs/scene-glossary.md)의 공식 명칭을 사용한다. README, 설계·검증 문서, 이미지 설명, PR 설명에도 같은 기준을 적용한다.
- 사용자는 빠른 구현 요청에서 “척추”, “지하 장치”, “비늘”, “꼬리” 같은 별칭을 사용할 수 있다. 용어집으로 대상을 해석하고 문서에는 본 컬럼, 리액터·리액터 챔버, 스케일 패널, 리본 트레일처럼 역할이 분명한 명칭을 쓴다.
- 문서 정리만으로 코드 식별자, 파일명, URL, 과거 측정값과 캡처 기준을 바꾸지 않는다. 본 컬럼을 설명하면서 실제로 사용하지 않는 `THREE.Bone`, `Skeleton`, 스킨드 메시 리깅을 구현했다고 주장하지 않는다.

## 한국어 블로그 편집

- `docs/troubleshooting/`의 원고와 설명 그림을 수정할 때는 [한국어 기술 블로그 편집 스킬](docs/troubleshooting/editorial/korean-tech-blog-editor/SKILL.md)을 읽는다. 증상과 구현 동작을 직접 쓰고, 그림의 주제목·박스·축 제목은 명사형으로 끝낸다.
- 코드·측정 조건·실제 캡처를 보존하고 편집 검사와 PC·모바일 렌더를 확인한다. 기존 릴리스를 덮어쓰지 않고 새 버전으로 보관한다.
